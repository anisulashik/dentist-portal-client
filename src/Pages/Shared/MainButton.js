import React from 'react';

const MainButton = () => {
    return (
        <div>
            <button className="btn btn-primary">Get Started</button>

        </div>
    );
};

export default MainButton;